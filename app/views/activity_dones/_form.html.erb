<%= form_with(model: activity_done, local: true) do |form| %>
  <% if activity_done.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_done.errors.count, "error") %> prohibited this activity_done from being saved:</h2>

      <ul>
      <% activity_done.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :activity_done_user_id %>
  </div>

  <div class="field">
    <%= form.label :activity_id %>
    <%= form.text_field :activity_id, id: :activity_done_activity_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
